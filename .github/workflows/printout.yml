name: PRINTOUT

on:
  workflow_dispatch:

jobs:
  printHash:
    name: print hash of image
    runs-on: ubuntu-latest
    steps:
      - run: echo '### Hello world! `test` :rocket:' >> $GITHUB_STEP_SUMMARY
